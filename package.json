{
  "name": "trao-prep-kit-root",
  "version": "1.0.0",
  "private": true,
  "description": "The AI Interview Prep Kit - Full-Stack Application and Batch Evaluator",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"blue,green\" \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w client",
    "build:shared": "npm run build -w shared",
    "build:server": "npm run build -w shared && npm run build -w server",
    "build:client": "npm run build -w shared && npm run build -w client",
    "start": "npm run start -w server",
    "start:server": "npm run start -w server",
    "start:client": "npm run start -w client",
    "test": "vitest run && npm run test -w client",
    "test:backend": "vitest run",
    "test:client": "npm run test -w client",
    "test:watch": "vitest",
    "evaluate": "tsx server/src/cli/evaluate.ts"
  },
  "dependencies": {
    "typescript": "^5.7.3"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "tsx": "^4.19.3",
    "vitest": "^3.0.7"
  }
}
